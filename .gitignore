node_modules/
.DS_Store
Thumbs.db
*.log

# รายชื่อพนักงานที่ export มา — เป็นข้อมูลบุคคลขององค์กร ไม่ควรอยู่ใน repo สาธารณะ
# เอาไปรันใน SQL Editor ของ Supabase โดยตรง (สร้างใหม่ได้เสมอด้วย scripts/export-employees.cjs)
supabase/employees.sql
# ทำเนียบแยกตามแบรนด์ เช่น employees-hino.sql (scripts/export-employees-xlsx.cjs)
supabase/employees-*.sql

# รายชื่อผู้มีสิทธิ์เข้าหลังบ้าน (สร้างใหม่ได้ด้วย scripts/export-ins-staff.cjs)
supabase/ins-staff.sql
# เจ้าหน้าที่ฝั่งฮีโน่ (มีชื่อ-รหัสพนักงาน) — ตัวแก้โค้ดอยู่ใน migrate-2026-09-25-staff-brand.sql
supabase/ins-staff-hino.sql

# hash ของกุญแจผูก LINE (ตัวจริงอยู่ใน backend/.env ของเซิร์ฟเวอร์ในออฟฟิศ)
supabase/line-bind-secret.sql

# พ mapping (scripts/export-emp-owner.cjs)
supabase/ins-emp-owner.sql

# กรมธรรม์พนักงานที่ยังไม่หมดอายุ (scripts/export-active-policy.cjs)
# มีชื่อพนักงาน ทะเบียนรถ เลขตัวถัง = ข้อมูลส่วนบุคคล ห้ามขึ้น repo สาธารณะ
supabase/ins-active-policy.sql
supabase/*active-policy.sql

# ที่อยู่บ้านพนักงาน (scripts/export-emp-addr.cjs) — ข้อมูลส่วนบุคคล
supabase/ins-emp-addr.sql

# เบอร์มือถือเจ้าหน้าที่ประกัน — ข้อมูลส่วนบุคคล
# ตัวแก้โค้ดแยกอยู่ใน migrate-2026-09-23-submit-brand-join.sql (อันนั้นขึ้น repo ได้)
supabase/ins-staff-phone.sql
