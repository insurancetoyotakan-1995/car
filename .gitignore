node_modules/
.DS_Store
Thumbs.db
*.log

# รายชื่อพนักงานที่ export มา — เป็นข้อมูลบุคคลขององค์กร ไม่ควรอยู่ใน repo สาธารณะ
# เอาไปรันใน SQL Editor ของ Supabase โดยตรง (สร้างใหม่ได้เสมอด้วย scripts/export-employees.cjs)
supabase/employees.sql

# รายชื่อผู้มีสิทธิ์เข้าหลังบ้าน (สร้างใหม่ได้ด้วย scripts/export-ins-staff.cjs)
supabase/ins-staff.sql

# hash ของกุญแจผูก LINE (ตัวจริงอยู่ใน backend/.env ของเซิร์ฟเวอร์ในออฟฟิศ)
supabase/line-bind-secret.sql
